import styled from "styled-components";
import Card from "./Card";

const SlimContentCard = styled(Card)`
  position: relative;

  width: 350px;
  top: 40px;
  margin: 0 auto;

  box-shadow: 0 0 6px -4px #000;
`;

export default SlimContentCard